package ${groupId};

import org.junit.Test;
import org.junit.Assert;

public class WelcomeServletTest {
    @Test public void dummy(){
        Assert.assertEquals(true, Boolean.parseBoolean("true"));
    }
}
